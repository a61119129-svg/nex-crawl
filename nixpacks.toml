[phases.setup]
nixPkgs = ["python313", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt", "pip install -e ."]

[start]
cmd = "uvicorn nexcrawl.api:app --host 0.0.0.0 --port ${PORT:-8000}"
